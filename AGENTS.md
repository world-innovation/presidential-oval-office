## Cursor Cloud specific instructions

This repository is currently empty (contains only a placeholder file `Goki-Room`). There is no application code, no dependency manifests, no build system, and no services to run.

Once application code is added, this section should be updated with:
- Tech stack and framework details
- How to install dependencies
- How to run lint, tests, build, and dev server
- Any non-obvious caveats for development
